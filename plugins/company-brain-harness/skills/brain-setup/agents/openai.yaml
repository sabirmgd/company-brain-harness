interface:
  display_name: "Brain Setup"
  short_description: "Create the starter brain scaffold and choose the operating profile."
  default_prompt: "Use $brain-setup when I say create the brain files, generate the scaffold, initialize a brain root, choose the simple team model, or run the setup step."
policy:
  allow_implicit_invocation: true
