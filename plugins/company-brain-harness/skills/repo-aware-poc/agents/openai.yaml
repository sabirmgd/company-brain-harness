interface:
  display_name: "Repo Aware POC"
  short_description: "Align prototypes to repo conventions."
  default_prompt: "Use $repo-aware-poc to plan this prototype."
policy:
  allow_implicit_invocation: true
