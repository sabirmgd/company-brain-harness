interface:
  display_name: "Repo Aware POC"
  short_description: "Align POCs and prototypes with existing repos."
  default_prompt: "Use $repo-aware-poc when I say build a POC, prototype this, where should this code live, check existing repos, or use our stack."
policy:
  allow_implicit_invocation: true
