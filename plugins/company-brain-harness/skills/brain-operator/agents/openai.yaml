interface:
  display_name: "Brain Operator"
  short_description: "Run today's check, drop-zone digest, queue review, and next actions."
  default_prompt: "Use $brain-operator when I say run today's brain check, do the morning review, check the queue, what needs review, process ADD_TO_BRAIN, or I added something to the company brain folder."
policy:
  allow_implicit_invocation: true
