interface:
  display_name: "Brain Operator"
  short_description: "Run today's check, queue review, and next actions."
  default_prompt: "Use $brain-operator when I say run today's brain check, do the morning review, check the queue, or what needs review."
policy:
  allow_implicit_invocation: true
