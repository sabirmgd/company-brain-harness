interface:
  display_name: "Brain Lint"
  short_description: "Find stale notes, broken links, and contradictions."
  default_prompt: "Use $brain-lint when I say what is outdated, find stale notes, check broken links, or can we rely on this."
policy:
  allow_implicit_invocation: true
